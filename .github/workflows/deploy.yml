name: remote ssh command
on: [push]
jobs:

  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: executing remote ssh commands using password
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: 82.180.137.15
          username: root
          password: ${{ secrets.PASSWORD }}
          port: 22
          script: cd /www/wwwroot/mev.2025fc.xyz/mev-bot-frontend && git pull --rebase origin main && npm install && git checkout -f . && npm run build